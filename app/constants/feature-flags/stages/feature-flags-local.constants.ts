import { FEATURE_FLAGS_DICT } from "../feature-flags"

export const FEATURE_FLAGS_LOCAL: typeof FEATURE_FLAGS_DICT = {
  "use-crypto-api-fetch-mock-class": true,
}

module.exports = FEATURE_FLAGS_LOCAL
